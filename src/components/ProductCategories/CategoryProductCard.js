const CategoryProductCard = ({ img, newProduct, title, text, dest }) => {
	return (
		<div className='w-full h-[42.75rem] border border-green-600'>
			{/* img */}
			{/* if new product, display new product text */}
			{/* product title */}
			{/* product text */}
			{/* product link/dest */}
		</div>
	);
};

export default CategoryProductCard;
